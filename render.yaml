services:
  - type: web
    name: fashion-cnn-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT --app-dir fashion
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
    healthCheckPath: /stats
